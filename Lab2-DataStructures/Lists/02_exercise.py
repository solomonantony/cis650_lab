# demonstrate the use of for, to iterate over a list's elements
measures = [10, 10.1, 9.9, 9.6, 10.1, 9.9, 9.8, 9.9, 10.3, 10]

# 1. Given some data, compute the average using only addition and division operations

import math
print(math.sqrt(36))
#. Given the the data, compute the standard deviation using only addition, division and the square root function
